people = ["Abe", "Bill", "Charles", "Dolly", "David", "Evelyn", "Frank", "Gunther"]

# comp for names that start with D
# a = [<map expression> for person in people <filter expression>]
# a = [name for name in people if name.startswith("D")]
# print(a)
# b = []

# for name in people:
#     if name.startswith("D"):
#         b.append(name)
# print(b)


# comp for names that end in Y

# comp for names that start with B through D
# d = [name for name in people if name.startswith("B")]
# print(d)

# comp for names in uppercase
# c = [name.upper() for name in people]
# print(c)